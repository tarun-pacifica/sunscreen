# Add your own tasks in files placed in lib/tasks ending in .rake,
# E.G. lib/tasks/capistrano.rake, they will automatically be available to Rake.

require_relative "config/application"

Rails.application.load_tasks
