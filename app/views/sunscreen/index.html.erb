<%= image_tag 'slip_logo.png' %>

<%= form_tag sunscreen_forecast_path, method: 'put', remote: true do %>
	<%= select_tag "city",options_from_collection_for_select(City.all, :id, :name) %>
	<br>
	<%= submit_tag "Slip Slop or Slap?", :id => "sun-submit" %>
<% end %>
